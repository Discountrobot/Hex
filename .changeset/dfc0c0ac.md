---
"hex-app": minor
---

Add pi coding agent integration (Agent Plugins → pi). Generates a TypeScript extension for ~/.pi/agent/extensions/ that bridges pi's agent_end, tool_call, and a new ask_user tool to Hex's voice window, mirroring the existing Claude Code hooks.
